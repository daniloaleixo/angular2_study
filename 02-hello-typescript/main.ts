var minhaVar = 'minha variavel';

function minhaFunc(x, y){
	return x + y;
}

let num = 1;
const bla = 0;

var numeros = [1,2,3];

numeros.map((val)=>{
	return val + 1;
});

numeros.map(val => val + 2)


class Matematica {
	soma(x, y){
		return x + y;
	}
}

var n1: string = 'dsadasasdsa';